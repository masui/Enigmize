* 非同期処理を奇麗に書く

* 拡張子を .enigma_remover とか .enigma_ink とかにする

* 正しいメアドの判定

* メッセージを入力できるようにする

------------------------

* 鍵が異なるときなどのエラー処理

* クリックですぐに謎データをメールで送れるようにする

* これで大丈夫か確認
    //a.css('display','none') // bodyにappendしなくても動くようだ?
    //$('body').append(a)
    a[0].click(); // jQueryの場合こういう処理が必要
    //$('body').remove(a)
  たぶん大丈夫そう
